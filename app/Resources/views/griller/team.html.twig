{% extends('layout.html.twig') %}

{% block body %}

	<div class="container">
		<div class="">
			<div class="page-header margin-none">
				<h2 class="padding-none">{{ team }}<br /></h2>
			</div>
			<div class="table-responsive">
<table class="table table-striped table-hover">
    {% for griller in grillers %}
    <tr><th>Name: {{griller.name}}</th></tr>
    <tr><td align="center"><img src="{{asset('uploads/photos/' ~ griller.photo)}}" width=200 /></td></tr>
    <tr><td>Phone: {{griller.phone}}</td></tr>
    <tr><td>Instagram: {{griller.instagram}}</td></tr>
    <br />
    {% endfor %}
</table>
</div>
</div>
</div>
{%endblock %}