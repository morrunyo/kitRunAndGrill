{% extends('layout.html.twig') %}

{% block body %}

	<div class="container">
		<div class="">
			<div class="page-header margin-none">
				<h2 class="padding-none">Gallery<br /></h2>
				<h3 colour="red">Aunque no se cargeun todas las fotos por la lentitud de la conexion, están almacenadas y las dejare en un zip la semana que viene en Facebook para que se puedan descargar.</h3>
			</div>
			<div class="table-responsive">
<table class="table table-striped table-hover">
    {% for image in images %}
        {% for filename in image.filenames|split(' ') %}
            {% if filename|length > 0 %}
               <tr><td align="center"><img src="{{asset('uploads/gallery/' ~ filename)}}" width=300 /></td></tr>
            {% endif %}
        {% endfor %} 
    {% endfor %}
</table>
</div>
</div>
</div>
{%endblock %}